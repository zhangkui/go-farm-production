# BUG-005 复现说明

## 现象与正确行为

农场资料编辑允许把总面积改成小于现有地块面积合计的数值。例如农场已有一个 80 亩地块，总面积从 100 亩改成 50 亩仍会保存成功，导致农场容量与地块数据互相矛盾。调整名称、地址或正常扩大面积应继续可用，但缩减后的总面积不得低于该农场全部地块的面积总和，失败时原资料必须保持不变。

## 固定验证

go test ./scripts/verify -count=1 -run '^TestBug005_BusinessRegression$'

该命令在当前缺陷分支稳定到达目标业务失败断言。Diagnosis 仅定位生产代码文件、符号、失效机制和调用链，不修改 Go 生产代码或测试。
