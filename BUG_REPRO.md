# BUG-008 复现说明

## 现象与正确行为

生产人员发现，正在执行的种植计划所属种植季可以从管理端直接停用。停用成功后，计划仍处于生长状态，却挂在一个不可用的种植季下。未被活动计划使用的种植季可以正常停用；只要仍有计划处于有效生产阶段，就应拒绝停用并保持原状态。

## 固定验证

go test ./scripts/verify -count=1 -run '^TestBug008_BusinessRegression$'

修复前稳定到达目标业务失败断言；修复后应全绿。不得修改、跳过或放宽测试。
