# BUG-014 复现说明

## 现象与正确行为

删除仍被业务数据引用的作物品种时表现不一致：被种植计划引用时失败后品种会被改成停用，被库存记录引用时又返回数据库内部错误，而不是统一的业务冲突。请只读检查删除前后的状态动作、引用判断、仓储调用和外键错误转换，说明具体文件、符号、调用顺序、失效机制以及持久化副作用的证据。本题仅做原因分析，不允许修改、创建或删除任何项目文件。

## 固定验证

go test ./scripts/verify -count=1 -run '^TestBug014_BusinessRegression$'

该命令在当前缺陷分支稳定到达目标业务失败断言。Diagnosis 仅定位生产代码文件、符号、失效机制和调用链，不修改 Go 生产代码或测试。
