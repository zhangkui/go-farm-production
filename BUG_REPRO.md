# BUG-001 复现说明

## 现象

同一投入品批次被多个农事任务领用后，退回操作可能错误共享其他任务的可退数量；损耗数量也可能被视为可退数量。并发发起两次退回时，两个请求还可能同时成功，使批次剩余量超过实际可恢复数量。

## 触发条件

1. 同一批次存在不同任务的领用记录。
2. 对其中一个任务提交超过该任务真实领用余额的退回。
3. 或者批次存在损耗记录后提交退回。
4. 或者对同一任务余额并发提交两个退回请求。

## 实际结果

不应成功的退回被接受，或两个并发请求同时成功，导致退回记录与批次库存不一致。

## 正确结果

退回额度只来自同一批次、同一任务尚未退回的真实领用量；损耗不可退；并发请求必须串行保持库存和退回记录一致。
