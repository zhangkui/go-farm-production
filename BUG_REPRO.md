# BUG-015 复现说明

## 现象与正确行为

创建种植季时，日期输入存在两种异常：带时区的 RFC3339 开始时间保存后自然日会偏移一天，开始日期和结束日期相同的零长度区间也会被接受。普通自然日范围仍需保持兼容，合法区间的日期必须按用户提交的日历日期保存，结束日必须严格晚于开始日。

## 固定验证

go test ./scripts/verify -count=1 -run '^TestBug015_BusinessRegression$'

修复前稳定到达目标业务失败断言；修复后应全绿。不得修改、跳过或放宽测试。
