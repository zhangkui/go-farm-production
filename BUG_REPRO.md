# BUG-032 复现说明

同一账号在多台设备登录后，账号停用、管理员重置密码或用户自行修改密码，都必须立即使变更前签发的全部刷新令牌失效。旧令牌不得继续换取新令牌，账号重新启用后也不得恢复有效；失败刷新不得创建新令牌或留下成功审计。账号保持启用并使用新密码重新登录后，正常刷新轮换必须继续可用。

固定验证命令：

`go test ./scripts/verify -count=1 -run '^TestBug032_BusinessRegression$'`
