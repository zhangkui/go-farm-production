# BUG-002 复现说明

## 现象

审计日志页面同时填写操作人、动作类型、业务对象、对象编号和时间范围后，结果会混入不相关记录，分页总数也会被放大。相同起止时间的查询不能稳定只命中目标边界记录。

## 触发步骤

1. 准备多条操作人、动作、业务对象、对象编号和时间不同的审计日志。
2. 通过审计日志查询接口同时填写多个筛选条件。
3. 使用相同的起止时间查询边界记录。
4. 运行固定公开回归测试：

```text
go test ./scripts/verify -count=1 -run '^TestBug002_BusinessRegression$'
```

## 实际结果

多条件查询按并集扩大结果集，分页 `total` 包含条件范围外记录；边界查询返回多条记录。Pre 的目标断言包括 `combined total=6 pages=6, want 1/1` 和 `boundary total=2 IDs=[2 1], want target 1`。

## 正确结果

所有已填写条件必须求交集，分页总数只能统计交集结果；时间范围包含端点，相同起止时间只命中目标记录；非法对象条件和倒置时间范围直接返回参数错误，合法单条件查询保持正常。
