# BUG-031 复现说明

## 现象与正确行为

同一个种植计划下存在多条采收记录时，只审核其中一条会连带改变其他记录的审核状态。即使把同计划的另一条记录恢复为未审核，只要该计划下仍有其他已审核记录，未审核记录也可以创建农产品库存并留下成功审计。

审核状态必须属于具体采收记录：审核哪条只影响哪条；未审核记录必须拒绝入库且不得留下库存记录或成功审计；已审核记录的正常入库和其他计划的数据隔离必须保持正常。

## 固定验证

go test ./scripts/verify -count=1 -run '^TestBug031_BusinessRegression$'

修复前稳定到达目标业务失败断言；修复后同一命令全绿。不得修改、跳过或放宽测试。
